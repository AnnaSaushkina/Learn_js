// Задача 1: Сравнение чисел

// Напишите код, который сравнивает два числа и выводит в консоль, какое из них больше, меньше или равны ли они.

let a = 5;
let b = 10;

function biggerNum(a, b) {
    if (a > b) {
        return a
    }
    else if (a < b) {
        return b
    }
    else return('numbers are equal')
}
console.log(biggerNum(a, b))

// Задача 2: Проверка типов

// Создайте две переменные, одну с числом, а другую со строкой, содержащей то же самое число. Используйте оператор строгого равенства (===) и нестрогого равенства (==), чтобы проверить, равны ли они, и выведите результаты в консоль.

let num = 5;
let str = "5";

// Ваш код здесь


// Задача 3: Возраст

// Напишите функцию checkAge, которая принимает возраст (число) и возвращает строку: "Вы совершеннолетний", если возраст больше или равен 18, и "Вы несовершеннолетний" в противном случае.

function checkAge(age) {
    // Ваш код здесь
}

// Примеры вызова функции
console.log(checkAge(20)); // "Вы совершеннолетний"
console.log(checkAge(16)); // "Вы несовершеннолетний"


// ▎Задача 4: Сравнение строк

// Создайте две строки и сравните их с помощью операторов сравнения. Выведите в консоль результат сравнения на равенство и неравенство.

let str1 = "Hello";
let str2 = "hello";

// Ваш код здесь


// ▎Задача 5: Проверка четности

// Напишите функцию isEven, которая принимает число и возвращает true, если оно четное, и false, если нечетное. Используйте оператор остатка от деления (%) для проверки.

function isEven(num) {
    // Ваш код здесь
}

// Примеры вызова функции
console.log(isEven(4)); // true
console.log(isEven(5)); // false


// ▎Задача 6: Сравнение массивов

// Создайте два массива с одинаковыми значениями и сравните их с помощью операторов == и ===. Объясните результаты.

let arr1 = [1, 2, 3];
let arr2 = [1, 2, 3];

// Ваш код здесь


// ▎Задача 7: Сравнение объектов

// Создайте два объекта с одинаковыми свойствами и значениями. Попробуйте сравнить их с помощью операторов == и ===. Объясните, почему результат может быть неожиданным.

let obj1 = { name: "Alice" };
let obj2 = { name: "Alice" };

// Ваш код здесь