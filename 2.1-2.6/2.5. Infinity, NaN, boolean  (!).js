// ### Задача 1: Infinity
// Напишите функцию isInfinite, которая принимает число и возвращает true, если оно равно Infinity или -Infinity, и false в противном случае.

function isInfinite(value) {
  // Ваш код здесь
}
console.log(isInfinite(10));
console.log(isInfinite(Infinity));
console.log(isInfinite(-Infinity));

// ### Задача 2: NaN

// Создайте функцию isNotANumber, которая принимает значение и возвращает true, если это значение NaN, и false в противном случае. Используйте функцию Number.isNaN.

function isNotANumber(value) {
  // Ваш код здесь
}

// ### Задача 3: Проверка ложных значений
// Напишите функцию isFalsy, которая принимает значение и возвращает true, если это значение является ложным в контексте JavaScript (т.е. false, 0, "", null, undefined, или NaN), и false в противном случае.

function isFalsy(value) {
  // Ваш код здесь
}

// ### Задача 4: Комбинированная проверка
// Создайте функцию checkValue, которая принимает значение и возвращает строку с описанием: "infinite", "not a number", "falsy value" или "truthy value" в зависимости от того, является ли значение Infinity, NaN, ложным или истинным.

function checkValue(value) {
  // Ваш код здесь
}

// * [ ] задачи по самим типам. 2.5
